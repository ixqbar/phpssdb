// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ssdb", "for ssdb support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ssdb", "enable ssdb support", "no");

if (PHP_SSDB != "no") {
	EXTENSION("ssdb", "ssdb.c");
}

